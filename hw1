/* 1. Арифметические операции:

a) Напишите код, который сложит два числа (например, 5 и 7) и выведет результат на консоль. */
let sum1 = 5 + 7;
console.log("Сумма 5 и 7 =", sum1);



/* 1. Арифметические операции:

b) Напишите код, который умножит два числа (например, 3 и 4) и выведет результат на консоль. */
let mult = 3 * 4;
console.log("Произведение 3 и 4 =", mult);



/* 2. Ветвление:

a) Напишите код, который проверит, является ли число четным. Если число четное, выведите на консоль сообщение "Число четное", иначе выведите "Число нечетное". */
let num = 8;
console.log(num % 2 === 0 ? "Число четное" : "Число нечетное");



/* 2. Ветвление:

b) Напишите код, который проверит, является ли год високосным. Если год високосный, выведите на консоль сообщение "Год високосный", иначе выведите "Год не високосный". */
let year = 2024;
let isLeap = (year % 4 === 0 && year % 100 !== 0) || (year % 400 === 0);
console.log(isLeap ? "Год високосный" : "Год не високосный");



/* 3. Циклы:

a) Напишите код, который выведет на консоль числа от 1 до 10. */
for (let i = 1; i <= 10; i++) {
    console.log(i);
}



/* 3. Циклы:

b) Напишите код, который выведет на консоль сумму всех чисел от 1 до 100. */
let total = 0;
for (let i = 1; i <= 100; i++) {
    total += i;
}
console.log("Сумма чисел от 1 до 100 =", total);



/* 4. Массивы:

a) Создайте массив чисел [1, 2, 3, 4, 5]. Напишите код, который выведет на консоль сумму всех элементов массива. */
let arrNums = [1, 2, 3, 4, 5];
let arrSum = arrNums.reduce((acc, val) => acc + val, 0);
console.log("Сумма элементов массива =", arrSum);



/* 4. Массивы:

c) Создайте массив чисел [1, 2, 3, 4, 5]. Напишите код, который выведет на консоль только четные элементы массива. */
let evenNums = arrNums.filter(n => n % 2 === 0);
console.log("Четные элементы массива =", evenNums);



/* 5. Объекты:

a) Создайте объект с информацией о человеке: имя, возраст, место жительства. Напишите код, который выведет на консоль всю информацию о человеке. */
let person = { name: "Иван", age: 30, city: "Минск" };
console.log("Информация о человеке:", JSON.stringify(person)); 



/* 5. Объекты:

c) Создайте объект с информацией о книге: название, автор, год издания. Напишите код, который выведет на консоль информацию о книге в формате "Название книги - Автор (Год издания)". */
let book = { title: "Мастер и Маргарита", author: "М. Булгаков", year: 1967 };
console.log(`${book.title} - ${book.author} (${book.year})`);



/* 6. Функции:

a) Напишите функцию, которая принимает два числа в качестве аргументов и возвращает их сумму. */
function sum(a, b) {
    return a + b;
}
console.log("Сумма 10 и 15 =", sum(10, 15));



/* 6. Функции:

c) Напишите функцию, которая принимает строку в качестве аргумента и возвращает ее в обратном порядке. */
function reverseString(str) {
    return str.split("").reverse().join("");
}
console.log("Обратная строка:", reverseString("Привет"));



/* 7. Соляночка общая

a. Создание каталога товаров:

· Создайте массив объектов, где каждый объект представляет товар (название, цена, количество на складе).

· Напишите функцию, которая принимает этот массив и возвращает новый массив, содержащий только товары, которых на складе меньше 5.

· Добавьте в каждый объект товара свойство discount, которое будет рассчитываться в зависимости от цены (например, скидка 10% для товаров дороже 1000 рублей). */
let products = [
    { name: "Телефон", price: 1500, stock: 3 },
    { name: "Ноутбук", price: 2500, stock: 10 },
    { name: "Мышь", price: 500, stock: 2 }
];
function lowStock(arr) {
    return arr.filter(p => p.stock < 5);
}
products.forEach(p => p.discount = p.price > 1000 ? p.price * 0.1 : 0);
console.log("Товары с малым количеством:", JSON.stringify(lowStock(products)));
console.log("Каталог с учетом скидок:", JSON.stringify(products));



/* 7. Соляночка общая

b. Создание социальной сети (упрощенная версия):

· Создайте объект User с полями name, age, friends (массив объектов других пользователей).

· Напишите функцию, которая принимает массив пользователей и возвращает пользователя с наибольшим количеством друзей.

· Реализуйте функцию, которая добавляет нового друга пользователю. */
let users = [
    { name: "Аня", age: 25, friends: [{}, {}, {}] },
    { name: "Петя", age: 30, friends: [{}] },
    { name: "Саша", age: 20, friends: [{}, {}, {}, {}] }
];
function mostFriends(arr) {
    return arr.reduce((max, u) => u.friends.length > max.friends.length ? u : max);
}
console.log("Пользователь с наибольшим количеством друзей:", mostFriends(users).name);



/* 7. Соляночка общая

c. Создание библиотеки книг:

· Создайте массив объектов, представляющих книги с свойствами название, автор и год издания. Используйте метод filter(), чтобы получить книги, изданные после 2000 года.

· Напишите функцию, которая принимает массив объектов с товарами (название и цена) и возвращает общую стоимость всех товаров. */
let books = [
    { title: "Book1", author: "Author1", year: 1999 },
    { title: "Book2", author: "Author2", year: 2005 }
];
let recentBooks = books.filter(b => b.year > 2000);
console.log("Книги после 2000 года:", JSON.stringify(recentBooks));

function totalPrice(items) {
    return items.reduce((sum, item) => sum + item.price, 0);
}
console.log("Общая стоимость товаров:", totalPrice(products));

